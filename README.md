### Setup
Similar to the IRED setup

### Training and Evaluation
Run `main.py`. The configurations are specified in `configs/configs.yaml`

### Implementation of sequential EBMs training and inference algorithms
See `model.py` (currently working...)
